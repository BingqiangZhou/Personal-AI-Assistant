================================================================================
                        个人AI助手 - Docker部署说明
================================================================================

📦 你要部署的是: 播客订阅 + AI总结功能的后台服务器
⏱️ 部署时间: 3-5分钟
🎯 难度: 简单 (会复制粘贴命令即可)
🔐 安全性: 中等 (适合个人/开发环境)

================================================================================
                              两种部署方式
================================================================================

【方式1】Docker Compose (⭐⭐⭐⭐⭐ 推荐 - 最快最简单)

  只需要3步:

  1. 配置环境 (30秒)
     cd backend
     notepad .env
     # 设置 SECRET_KEY 和数据库密码

  2. 启动 (复制粘贴)
     docker compose -f docker-compose.podcast.yml up -d --build

  3. 等待完成 (1-3分钟)
     访问 http://localhost:8000/docs

【方式2】手动启动 (适合开发者)

  详细步骤见: README_DOCKER_DEPLOY.md (搜索"手动运行")

================================================================================
                              立即开始
================================================================================

如果你已安装 Docker，直接运行以下命令:

-------------------------------------------------
cd E:\Projects\AI\PersonalKnowledgeLibrary\Claude\personal-ai-assistant
start.bat
-------------------------------------------------

start.bat 会:
✅ 检查Docker环境
✅ 引导配置.env文件
✅ 自动构建和启动
✅ 显示状态和访问地址

================================================================================
                              当前文件说明
================================================================================

docker-compose.podcast.yml   ⭐ Docker配置文件 (已优化)
start.bat                    ⭐ Windows一键启动脚本
README_DOCKER_DEPLOY.md      详细部署文档
DEPLOY_DOCKER.md             Docker部署问题排查
DOCKER_ANALYSIS.md           两种方案对比分析
QUICK_DEPLOY.txt             极简快速命令清单

backend/
  run_all_tests.py           测试脚本
  tests/                     归类后的测试文件
  database_migration.py      数据库迁移

================================================================================
                              验证成功
================================================================================

部署成功后，你应该看到:


   访问地址: http://localhost:8000/docs
   健康检查: http://localhost:8000/health
   API端口: 8000


测试步骤:
1. 打开浏览器访问 http://localhost:8000/docs
2. 注册用户 (POST /api/v1/auth/register)
3. 测试播客订阅 (POST /api/v1/podcasts/subscription)
4. 查看单集 (GET /api/v1/podcasts/episodes)

================================================================================
                              停止服务
================================================================================

docker compose -f docker-compose.podcast.yml down

================================================================================
                              常用命令
================================================================================

查看状态:  docker compose -f docker-compose.podcast.yml ps
查看日志:  docker compose -f docker-compose.podcast.yml logs -f backend
重启服务:  docker compose -f docker-compose.podcast.yml restart
停止服务:  docker compose -f docker-compose.podcast.yml down

================================================================================
                              需要帮助?
================================================================================

查看详细文档: README_DOCKER_DEPLOY.md
查看问题排查: DEPLOY_DOCKER.md
对比分析: DOCKER_ANALYSIS.md

如果部署失败，请运行以下命令获取帮助信息:
1. docker --version
2. docker compose -f docker-compose.podcast.yml ps
3. docker compose -f docker-compose.podcast.yml logs backend

================================================================================
                                       祝你部署顺利！
================================================================================
