================================================================================
                    个人AI助手 - Docker部署快速指南
================================================================================

📁 文件位置: E:\Projects\AI\PersonalKnowledgeLibrary\Claude\personal-ai-assistant\docker\

================================================================================
🚀 最快启动方式 (3分钟)
================================================================================

双击运行:  scripts\start.bat

或者手动执行:
  cd docker
  docker compose -f docker-compose.podcast.yml up -d --build

┌───────────────────────────────────────────────────────────────┐
│ 完成后访问: http://localhost:8000/docs                        │
│ 查看健康:   curl http://localhost:8000/health                 │
└───────────────────────────────────────────────────────────────┘

================================================================================
📋 部署前准备 (1分钟)
================================================================================

1. 确保 Docker Desktop 正在运行 (图标为绿色)

2. 配置环境变量 (只需设置1个值):

   打开: E:\Projects\AI\PersonalKnowledgeLibrary\Claude\personal-ai-assistant\backend\.env

   设置: SECRET_KEY=这里填写安全的随机字符串

   生成命令:
   python -c "import secrets; print(secrets.token_urlsafe(48))"

================================================================================
📁 文件说明
================================================================================

核心文件:
  docker-compose.podcast.yml    ← 配置文件 (必看)
  scripts/start.bat            ← Windows一键启动脚本
  README.md                    ← 入口文档

详细文档:
  部署说明.txt                  ← 中文快速指南 (从这里开始)
  README_DOCKER_DEPLOY.md      ← 完整手册
  DEPLOY_DOCKER.md             ← 问题排查
  DOCKER_ANALYSIS.md           ← 技术分析
  QUICK_DEPLOY.txt             ← 命令速查
  INDEX.md                     ← 导航索引

================================================================================
🔍 验证部署是否成功
================================================================================

1. 检查服务状态
   docker compose -f docker-compose.podcast.yml ps

   期望看到:
   NAME                 STATUS
   podcast_postgres     Up
   podcast_redis        Up
   podcast_backend      Up

2. 健康检查
   请在浏览器中打开: http://localhost:8000/health

   应返回: {"status": "healthy"}

3. 访问文档
   请在浏览器中打开: http://localhost:8000/docs

   应看到 FastAPI 交互式文档

================================================================================
🛠️ 常用管理命令
================================================================================

查看状态:  docker compose -f docker-compose.podcast.yml ps
查看日志:  docker compose -f docker-compose.podcast.yml logs -f backend
重启服务:  docker compose -f docker-compose.podcast.yml restart backend
停止服务:  docker compose -f docker-compose.podcast.yml down

(加 -v 可删除数据: down -v)

================================================================================
⚠️ 常见问题
================================================================================

❌ 端口冲突: Address already in use
   ✅ 解决: 停止占用8000端口的程序，或修改 docker-compose.yml 中的端口映射

❌ 连接失败: Connection refused
   ✅ 解决: 等待1分钟再试，或检查日志: logs backend

❌ 密码错误: password authentication failed
   ✅ 解决: 确保 backend/.env 中的数据库连接字符串匹配 docker-compose.podcast.yml

❌ 未找到Docker
   ✅ 解决: 下载安装 Docker Desktop: https://www.docker.com/products/docker-desktop/

================================================================================
📖 需要更详细的帮助？
================================================================================

如果遇到问题，请查看: DEPLOY_DOCKER.md
如果想了解技术细节: DOCKER_ANALYSIS.md
如果是Windows用户: 双击 scripts\start.bat 有图形指引

================================================================================
✅ 部署成功标志
================================================================================

✓ 3个服务全部显示 "Up"
✓ http://localhost:8000/health 返回 "healthy"
✓ http://localhost:8000/docs 正常显示
✓ 能注册/登录用户
✓ 能添加播客订阅

================================================================================
祝部署顺利！🎉
================================================================================
