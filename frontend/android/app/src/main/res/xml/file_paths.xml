<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External cache path for APK downloads -->
    <external-cache-path
        name="apk_cache"
        path="." />

    <!-- Cache path as fallback -->
    <cache-path
        name="cache"
        path="." />

    <!-- External files path for compatibility -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Files path for compatibility -->
    <files-path
        name="files"
        path="." />
</paths>
