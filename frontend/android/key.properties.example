# Android Keystore Configuration Example
# Android Keystore 配置示例文件
#
# Instructions / 说明:
# 1. Copy this file to 'key.properties' in the same directory
#    将此文件复制到同目录下的 'key.properties'
# 2. Fill in your keystore information
#    填写你的 keystore 信息
# 3. Add 'key.properties' to .gitignore (already done)
#    将 'key.properties' 添加到 .gitignore（已完成）
#
# Generate Keystore / 生成 Keystore:
# keytool -genkey -v -keystore release.keystore -alias release -keyalg RSA -keysize 2048 -validity 10000
#
# For Local Development / 本地开发:
# Use debug signing for easy installation during development
# 本地开发使用 debug 签名，方便安装测试

# Keystore file path (relative to this file)
# Keystore 文件路径（相对于此文件）
storeFile=../release.keystore

# Keystore password
# Keystore 密码
storePassword=your_keystore_password

# Key alias
# 密钥别名
keyAlias=release

# Key password
# 密钥密码
keyPassword=your_key_password
