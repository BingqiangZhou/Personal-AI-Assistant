#  ⚠️  将于 2025-12-18 废弃

# 此文件已过时，请使用新文件:
#
#   cd docker
#   然后查看 docker-compose.podcast.yml
#   或运行 scripts\start.bat
#
# 详细文档: docker/README.md

version: '3.8'
services:
  # 服务配置已迁移到:
  # E:\Projects\AI\PersonalKnowledgeLibrary\Claude\personal-ai-assistant\docker\docker-compose.podcast.yml
  #
  # 快速启动:
  # docker compose -f docker/docker-compose.podcast.yml up -d --build
  #
  # 或Windows双击: docker\scripts\start.bat

  note:
    image: alpine:latest
    command: |
      echo "请使用新的部署方式"
      echo "进入docker目录: cd docker"
      echo "查看文档: cat README.md"
    # 这个容器会立即退出，仅作为提醒
